# 流云记 · 鸿蒙开发日志

> 项目路径：`/Users/caixiaodan/liuyunji`
> 目标：爱助手（love-helper）Web 应用嵌入鸿蒙 WebView + 原生 TTS/ASR 能力

---

## 2026-05-08 — v0.7 模态滚动穿透修复 + 抽屉拖拽关闭 + 对话框滚动锁定

### 修复内容

**① 模态浮层滚动穿透（所有场景）**
- 新增 `window._锁定滚动()` / `window._解锁滚动()` 工具函数，通过控制 `#主内容区` 的 `overflow` 属性锁定/解锁滚动
- 覆盖全部模态弹窗：设置浮层、AI整理确认卡片、压缩对话框、删除会话确认、智能体权限向导（创建和删除）、`_自定义输入` / `_自定义确认` 对话框
- 确保打开弹窗时背后内容不再随之滚动

**② 抽屉拖拽滑动关闭**
- 在抽屉遮罩上绑定 touchstart/touchmove/touchend 事件
- 向右滑动抽屉 → 跟随手指位移 + 遮罩透明度同步
- 位移超过 25% 抽屉宽度 → 自动关闭（平滑动画）
- 不足 25% → 弹回原位
- 优先判定水平拖拽（8px 阈值 + 水平>垂直条件），不影响抽屉内竖滚

### 涉及文件
- `js模块/初始化.js`：+`_锁定滚动`/`_解锁滚动` + 自定义对话框滚动锁定
- `js模块/抽屉.js`：+拖拽关闭手势
- `js模块/抽屉底部.js`：设置按钮→锁定
- `js模块/设置浮层.js`：关闭→解锁
- `js模块/对话管理.js`：压缩对话框 + 删除会话确认锁定
- `js模块/备忘录UI.js`：AI整理确认卡片锁定
- `js模块/智能体权限向导.js`：创建/删除向导锁定

## 2026-05-08 — v0.7.1 操作栏按钮改用 touchend（第二轮修复）

### 问题

用户反馈第一轮修复后，第一次滑开点 📁 可以，但**卡片回归原位后再次滑开点击需要两次**。

### 第二轮根因

鸿蒙 WebView 的 `touchstart → touchend → click` 合成链在展开动画窗口内不可靠。第二次滑开后卡片 0.15s 展开动画+滑动状态变量重置（300ms），用户立即点 📁 时，touchend 在按钮上触发但 click 合成时目标可能已偏移，第一次 click 被丢弃。

### 修复

操作栏按钮直接监听 `touchend`（非 passive，调 `preventDefault()` 阻止 click 合成），触控设备上完全绕过 click 链路。桌面端保留 click 降级。

```js
function 操作栏触控处理(e) {
  e.preventDefault();
  e.stopPropagation();
  实际处理操作栏操作(e.currentTarget);
}

function 操作栏点击处理(e) {
  e.stopPropagation();
  if ('ontouchstart' in window) return;  // touch 设备跳过
  实际处理操作栏操作(e.currentTarget);
}
```

### 涉及文件
- `js模块/卡片滑动.js`（流云记 + love-helper 同步）

---

## 2026-05-08 — v0.7 修复展开卡片操作栏按钮点击两次问题（第二轮）

### 问题反馈

第一次修复（touchstart 跳过操作栏按钮）后，用户反馈：刚进 App 第一次滑开点图标可以，但**把卡片回归原位后，再次滑开点就需要两次**。

### 第二轮根因分析

第一次能工作 → 第二次不能 → 一定存在残留状态或事件链竞态。

**核心问题：touchstart → touchend → click 的延迟链在鸿蒙 WebView 中的不可靠性。**

即使我们在 touchstart 中跳过了操作栏按钮的收起逻辑，但浏览器在 touchend 后合成 click 的时机不确定——特别是在展开动画（0.15s）和状态变量重置（300ms 保护期）的窗口内。

第二次滑开时，`结束滑动逻辑` 设了 `忽略下次点击 = true; setTimeout(300ms)`，卡片同步加 `.展开` class。用户此时立即点击 📁 按钮：
1. touchstart → `开始滑动` → 展开分支 → 按钮检查通过 → return
2. touchend → `操作栏触控处理`（新方案）同步执行
3. click 可能被 `preventDefault` 阻止，或被 `'ontouchstart' in window` 跳过

由于 touchstart 在展开状态下只有按钮检查 + return，没有任何延迟，`touchend` 直接在按钮上触发（不经过卡片的 touch 监听），所以无需等到 click 合成。

### 第二轮修复方案

**核心思路**：在触控屏上直接用 `touchend` 响应操作栏按钮，绕过 click 合成链路。

改动：
1. 操作栏按钮新增 `touchend` 监听（非 passive，可 preventDefault）
2. 新增 `操作栏触控处理(e)`：调 `e.preventDefault()` 阻止 click 合成 + 直接执行操作
3. `操作栏点击处理(e)` 改为：触控设备上跳过（`'ontouchstart' in window`），仅桌面端使用
4. 抽离 `实际处理操作栏操作(按钮)` 给两者共享

```js
function 操作栏触控处理(e) {
  e.preventDefault();  // 阻止浏览器合成 click
  实际处理操作栏操作(e.currentTarget);
}

function 操作栏点击处理(e) {
  e.stopPropagation();
  if ('ontouchstart' in window) return;  // 触控设备跳过
  实际处理操作栏操作(e.currentTarget);
}
```

### 涉及文件
- `js模块/卡片滑动.js`（流云记 + love-helper 同步）

---

## 2026-05-08 — v0.7 ArkTS 严格模式编译错误修复

### 背景

在 `saveTextFile` + `saveFile` 桥接方法实现后，Index.ets 出现多轮编译报错。核心原因：ArkTS 严格模式（arkts-strict）对类型系统有非常具体的要求。

### 修复轮次

#### 第一轮：catch 具体类型（10 个问题）
- **报错**：`arkts-no-types-in-catch` × 5 + `Function may throw exceptions` × 2
- **根因**：全部 5 处 `catch (e: Error)` 写法不允许，ArkTS catch 子句不能带具体类型
- **修复**：`catch (e: Error)` → `catch (e)`；错误信息提取用 `String(e)` 兜底

#### 第二轮：in 操作符（4 个问题）
- **报错**：`arkts-no-in` × 2（`'message' in e`）+ arkts-strict-typing-required × 2（`// @ts-ignore`）+ Function may throw × 2
- **根因**：`in` 操作符在 ArkTS 中不支持；`// @ts-ignore` 注释也被禁止
- **修复**：
  - 删除 `hasMessage` 守卫函数（用了交叉类型 + 非法守卫）
  - 改为 `safeErrorMessage` 函数：先用 `instanceof Error` 判断，然后用 `(e as Record<string, Object>)['message']` 尝试取值
  - 删除全部 `// @ts-ignore`，改用显式 `as` 断言

#### 第三轮：catch 类型 + unknown（4 个问题）
- **报错**：`arkts-no-types-in-catch`（`catch (_e: unknown)`）+ `arkts-no-any-unknown` + Function may throw × 2
- **根因**：ArkTS 不允许 catch 带任何类型注解（包括 `unknown`）
- **修复**：`catch (_e: unknown)` → `catch (ee)`（无类型）

### 最终 ArkTS 安全写法总结

| 场景 | ❌ 错误写法 | ✅ 正确写法 |
|------|-----------|-----------|
| catch 子句 | `catch (e: Error)` / `catch (e: unknown)` | `catch (e)`（无类型注解） |
| 错误信息提取 | `err.message`（直接访问） | `String(e)` 或 `(e as Record<string,Object>)['message']` 加类型守卫 |
| 类型守卫 | `'prop' in obj` | 改用 `as` 断言 + try 或 `instanceof` |
| 关闭类型检查 | `// @ts-ignore` | 禁止使用，必须用 `as` 断言替代 |
| any/unknown | `let x: any` | 禁止使用，ArkTS 要求显式类型（`arkts-no-any-unknown`） |
| 交叉类型 | `A & B` | 改用接口继承或单接口合并字段 |
| 抛异常调用 | 仅 try 包着不够 | 加 const 中间变量（如 `const ctx: Type = this.field` 再调方法） |

### 尚未完全解决的谜题

`terminateSelf()` 和 `runJavaScript()` 虽已在 try 块内，但 ArkTS 编译器仍可能报 `Function may throw exceptions`。这是编译器对 API 声明 `@throws` 标记的严格检查。已尝试：加中间变量存引用、拆成两步。若仍报，可能需要 DevEco 手动标记或改工程配置。

---

## 2026-05-08 — v0.7 文件保存架构重构（saveTextFile + 消除冗余 base64 编码）

### 变更动机

文本类导出（JSON 导出、备份文件）在 JS 侧用 `btoa(unescape(encodeURIComponent(输出内容)))` 编码为 base64 再传给 ArkTS，ArkTS 再用 `Base64Helper.decodeSync` 解码回 UTF-8 字节写入文件。这一来一回的 base64 编解码完全多余，且增加了 33% 的跨桥传输量。

### 重构方案

| 场景 | 方法 | 数据流 |
|------|------|--------|
| **文本** (JSON/备份/智能体) | `saveTextFile(text)` | JS 原始字符串 → ArkTS `TextEncoder` → UTF-8 字节 → 写文件 |
| **二进制** (图片/ZIP/附件) | `saveFile(base64)` | JS base64 (自动去 `data:...;base64,` 前缀) → ArkTS `Base64Helper.decodeSync` → 写文件 |
| **降级** (无原生桥接) | Blob + a.click | 浏览器原生下载 |

### 改动文件

**流云记**:
- `Index.ets` — 新增 `saveTextFile` 方法（`new util.TextEncoder().encodeInto(text)`），`saveFile` 加 `_stripDataUrlPrefix` 兼容 dataURL，注册 `'saveTextFile'` 到 methodList
- `导出功能.js` — `_触发下载` 优先走 `saveTextFile`，加防重入锁 `_触发下载_保存锁`，加文件名安全过滤 `_安全文件名()`
- `数据备份.js` — 两处原生保存优先走 `saveTextFile`

**Love-helper** 同步：
- `导出功能.js` — 同上
- `数据备份.js` — 同上

### 审查意见落实

1. ✅ **dataURL 前缀去除** — `_stripDataUrlPrefix` 在 ArkTS `saveFile` 内自动处理
2. ✅ **消除冗余 base64 编码** — 文本走 `saveTextFile`，旧 `btoa(unescape(encodeURIComponent()))` 仅保留在 saveFile 降级分支
3. ✅ **防御性 JSON 解析** — 所有 `JSON.parse(res)` 外包 try-catch
4. ✅ **用户取消静默处理** — ArkTS 返回 `{success:false,error:'用户取消保存'}`，JS 不弹窗
5. ✅ **文件名安全** — `_安全文件名()` 替换 `/\\:*?"<>|` → `_`
6. ✅ **防重入锁** — `_触发下载_保存锁` 防止文件选择器重叠

### API 确认

鸿蒙 SDK API 22 中 `util.TextEncoder` 的正确用法：
- 构造：`new util.TextEncoder()`（非 `TextEncoder.create()`）
- 编码：`encoder.encodeInto(text)` 返回 `Uint8Array`

---

## 2026-05-07 — v0.4 图标替换 + 卡片右滑阻断 + 返回手势修复

### 标签选择页滑动返回不生效

**问题**：在编辑页面进入标签选择页后，鸿蒙边缘滑动返回手势失效。
**根因**：标签选择页 `position:fixed` 全屏覆盖 + `overflow-y:auto` + `-webkit-overflow-scrolling:touch`，WebView 内部的触摸事件被标签选择页的滚动逻辑消费，鸿蒙系统无法识别边缘返回手势，且 `onBackPress` 未被触发。
**第一次修复（失败）**：在标签选择页上添加左边缘触摸监听（20px 起始 + 30px 右滑阈值 + 横向优先），手动触发关闭。JS 层面监听的触摸事件未到达（鸿蒙手势层在 WebView 外就处理了），修复无效。
**第二次修复（待验证）**：CSS 添加 `touch-action: pan-y`，限制标签选择页只处理纵向滚动，横向手势交给鸿蒙系统层处理。

### 文件夹按钮变白

**问题**：边缘滑动关闭文件夹下拉菜单后，文件夹按钮（选择器）背景色变成白色。退出编辑页重新进入恢复正常。
**根因**：打开下拉菜单时设置了大量 inline styles（`position:fixed; background:white; border:1px solid #ccc;` 等），关闭时只移除了 `显示` class，所有 inline style 残留未清理。虽然 `display:none` 使下拉菜单不可见，但残留的 inline style 在后续可能干扰 CSS 变量渲染或 WebView 重排。
**修复**：关闭下拉菜单时清除所有与之相关的 inline styles，让元素彻底回到 CSS 定义状态。

### 文件夹下拉菜单在编辑页面被漏检

**问题**：编辑页面打开文件夹下拉菜单后，边缘滑动返回直接退到备忘录面板（预期：关闭下拉菜单，留在编辑页面）。
**根因**：`#文件夹选择按钮` 和 `#文件夹下拉菜单` 在 HTML 中是编辑页面的子元素，但 `_handleBack` 只在第 5 层（`备忘录面板` 条件块）内检测文件夹下拉菜单。编辑页面（第 4 层）根本没检测文件夹下拉，直接落到第 8 层切换面板。
**修复**：第 4 层（编辑页面专属弹层）中加入文件夹下拉菜单检测，位于颜色面板之后、标签选择页之前。同时保留第 5 层的检测作为备用。

---

## 2026-05-07 — v0.4 图标替换 + 卡片右滑阻断（原条目）

### v0.4 改动内容

| 功能 | 详情 |
|------|------|
| App 图标替换 | 素材图（兰花）替换 `background.png`/`foreground.png`/`startIcon` |
| 图标裁边 | 豆包生成的图片四周大量白边，使用 E-集中65% 方案裁剪后缩放到 1024x1024 |
| 应用名汉化 | AppScope `app_name` + entry `EntryAbility_label` 改为「流云记」 |
| 卡片右滑阻断 | 右滑不再触发操作栏（左滑逻辑不变），触摸/鼠标均生效 |

### 图标替换细节

- **源图**：桌面的 `流云记图标.png`（2048x2048），兰花位于画面中央，四周 610px/398px/418px/518px 渐变空白背景
- **测试方案**：生成 11 个裁剪版本放入桌面 `图标测试/` 文件夹供选择：A(紧裁) / B(撑满) / C(留边10%) / D(80%居中) / E(65%/75%/85%集中) / F/G/H(自适应边界)
- **选定方案**：E-集中65%，最集中画面主体
- **分层图标配置**：`layered_image.json` 指向 `background`(不透明完整图) + `foreground`(非白色区域保留，渐变背景变透明)

### 右滑阻断修复

**问题**：左滑/右滑都触发操作栏展开，右滑应该无反应。
**根因**：`触摸移动()` / `桌面鼠标移动()` 中 `deltaX > 0` 时仅视觉上把 `translateX` 设为 0，但 `是否发生了滑动` 仍为 true，`结束滑动逻辑()` 中不区分左右方向，只要距离够就展开。
**修复**：检测到 `deltaX > 0`（右滑）立即 `已判定为竖滚 = true` / `return`，完全阻断后续展开逻辑。

### 应用名汉化教训

桌面显示的 App 名由 **entry module.json5 的 label → entry string.json** 控制，跟 AppScope 的 `app_name` 是两码事。

---

## 2026-05-07 — v0.3 卡片滑动体验完善

### v0.3 改动内容

| 功能 | 详情 |
|------|------|
| 卡片滑动 v5 重构 | 方向判定阈值 8px + 滑动保护中标记 + 竖滚早期退出 |
| 竖滚零延迟 | 早期方向判定（8px）+ 竖滚判定后 touchmove 直接 return，浏览器原生处理 |
| 滑动误触修复 | 滑动保护中 300ms + 展开状态下点击只收起不进编辑 |
| 滑动起始Y | touchstart 记录起始Y，方向判定更精确 |
| `_当前展开的卡片()` 暴露 | click 处理器检测有无展开卡片 |
| `_是否发生了滑动()` 联合检测 | 同时检查原始标记 + 滑动保护中标记 |
| 智能体专属文件夹提示 | 系统提示词注入「你有一个与你同名的文件夹」 |
| 默认权限改为 none | 新建/无配置智能体默认无权限 |
| none 权限下允许创建 | mode:none 时创建备忘录不被拦截 |
| 富文本工具强制触发 | 操作关键词新增加粗/斜体/颜色/字体等富文本相关词 |
| update_memo 支持 HTML | 工具描述明确告知 AI 内容字段支持 HTML 标签 |
| 百度搜索恢复直连 | 去掉 localhost:3778 代理，直连 qianfan.baidubce.com |

### v0.3 关键修复链

#### 1. 竖滚不灵敏 vs 误触（同时解决两个问题）

**问题1**：左滑后点击其他地方误入编辑页面。
**根因**：`是否发生了滑动` 在 touchend 时立即被重置为 false，click 事件检测不到刚发生过滑动；且展开状态下点击其他卡片无保护。
**修复**：
- `滑动保护中` 标记保持 300ms，`_是否发生了滑动()` 联合检测
- `_当前展开的卡片()` 暴露给 click 处理器
- 卡片 click 增加检查：有展开卡片时 → 只收起，不进编辑
- `忽略下次点击` 从 200ms 延长到 300ms

**问题2**：竖滚感觉不跟手，手指滑了页面不动。
**根因**：`已判定为竖滚` 声明了但从未被设为 true，每个 touchmove 都走完 JS 判断才 return。
**修复**：新增 `滑动起始Y` + 8px 方向判定阈值，一旦 deltaY ≥ deltaX 就设 `已判定为竖滚 = true` 并直接 return。

#### 2. 智能体不知道自己的文件夹
**问题**：新智能体不知道有个同名文件夹属于它，也不知道能直接操作。
**修复**：`获取系统提示词()` 注入 2.15 节专属文件夹声明；`角色默认系统提示词` 说明同名文件夹使用权限。

#### 3. 权限默认改为 none
**修复**：`创建新智能体()` 的 memo_access 默认 mode 从 'all' 改为 'none'；`获取智能体权限()` 失败时同理返回 none。

#### 4. AI 不使用富文本工具
**问题**：AI 写 markdown 或纯文本，不调用设置颜色/加粗等工具。
**修复**：操作关键词列表新增加粗/斜体/颜色/字体/富文本/字号/样式/下划线/删除线；update_memo 工具 content 描述加 HTML 标签说明。

---

## 2026-05-07 — 项目启动 & 爱助手迁移

### 架构决策

**选定方案：WebView 嵌入（方案二）**
- 不重写 ArkTS 版，直接将 love-helper 的 HTML/JS/CSS 放入 `resources/rawfile/`
- 鸿蒙侧只负责 WebView 容器 + 后续通过 JSBridge 暴露原生能力（TTS/ASR）
- 无需过渡期，Web UI 仍可在浏览器独立开发调试

### 项目初始结构

```
liuyunji/
├── entry/src/main/
│   ├── ets/
│   │   ├── entryability/EntryAbility.ets
│   │   └── pages/Index.ets          ← WebView 容器
│   ├── resources/rawfile/
│   │   ├── index.html               ← 爱助手入口
│   │   ├── css/
│   │   │   ├── 样式.css
│   │   │   └── 智能体编辑面板-样式.css
│   │   └── js模块/                   ← 35 个 JS 模块
│   ├── module.json5                 ← 权限声明
│   └── resources/base/
└── ...
```

### 迁移操作记录

| 操作 | 详情 |
|------|------|
| 复制文件 | love-helper → rawfile，35 JS + 2 CSS + 1 HTML 全部对齐 |
| 网络权限 | `module.json5` 添加 `ohos.permission.INTERNET` |
| 重写 Index.ets | WebView 加载 `$rawfile('index.html')`，开启 `javaScriptAccess` + `domStorageAccess` |
| CSS 配置 | 修复 `EntryAbility.ets` ConfigurationConstant 导入（鸿蒙 API 15） |
| font-awesome | 注释掉（鸿蒙 rawfile 没有该文件） |
| navigator.onLine | 注入脚本强制 `true`（鸿蒙 WebView 默认可能 false，触发离线提示） |
| 创建开发快捷方式 | `流云记·开发测试.command` → HTTP server + Chrome 自动打开 |
| 百度搜索代理 | 将 `localhost:3778/proxy/baidu-search` 恢复为直连 `qianfan.baidubce.com/v2/ai_search/web_search` |

### 已知问题（待修复）

1. **百度联网搜索之前走代理** — 已修复为直连 API
2. **IndexedDB 数据不跨 origin** — `file://` 与 `http://localhost:3777` 数据库互相隔离
3. **font-awesome.min.css** — 暂时注释，图标可能缺失（但功能不受影响）

### 下一步计划

1. **TTS 原生能力** — 从 huawei6tts 项目引入 CoreSpeechKit textToSpeech（JSBridge 暴露）
2. **ASR 语音识别** — speechRecognizer 实现语音转文字输入
3. **后台音乐播放** — MediaSession + 后台进程保活（类似豆包通话体验）
4. **通话状态栏** — WebView 内通话悬浮窗 + 麦克风/扬声器切换

---

## 爱助手原项目日志

完整开发日志请见：
- `/Users/caixiaodan/Desktop/dev/love-helper/开发日志.md`（1328 行）
- `/Users/caixiaodan/Desktop/dev/love-helper/常见错误对照速查.txt`

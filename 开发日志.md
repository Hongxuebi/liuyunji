# 流云记 · 鸿蒙开发日志

> 项目路径：`/Users/caixiaodan/liuyunji`
> 目标：爱助手（love-helper）Web 应用嵌入鸿蒙 WebView + 原生 TTS/ASR 能力

---

## 2026-05-07（续）— 智能体权限体系 v0.2

### v0.2 改动内容

| 功能 | 详情 |
|------|------|
| 权限向导浮层 | 创建智能体后弹出三选项：①暂不开放 ②选择已有文件夹 ③创建同名专属文件夹 |
| 删除确认向导 | 删除智能体时弹窗：保留文件夹 / 删除关联文件夹及其备忘录 |
| 智能体重命名同步 | 编辑面板或右键重命名 → 自动重命名同名文件夹（IndexedDB + 内存同步） |
| 权限默认改为 none | 新建/无配置智能体默认 `mode:'none'`，安全优先 |
| none 权限修复 | 之前 `mode:none` 下连创建备忘录都被拦截，现改为允许创建 |
| 编辑弹窗样式 | 按钮加 `!important` 防覆盖、文本域 min-height 100px + overflow-y:scroll |
| font-awesome 恢复 | 原本注释，现已恢复 |
| 卡片滑动重构 | 全 passive + CSS touch-action:pan-y，竖滚零延迟 |
| 智能体权限向导.js | 新建文件 370 行，含创建向导 + 删除向导 + 钩子注入 |

### 关键修复

#### 1. 智能体重命名不同步文件夹
**问题**：编辑面板改名字后，智能体配置名改了，但叫原名的文件夹还是叫原名。

**原因**：`保存配置()` 只改了 `agent.json` 的 `name` 字段，没动文件夹。

**修复**：在两个入口（右键菜单 + `保存配置()`）检测 name 变化后，调用 `_重命名文件夹(旧名, 新名)`。

#### 2. none 权限下无法创建备忘录
**问题**：代码写的是 `工具名 !== '创建备忘录' && 权限.length === 0`，看起来允许创建，但实际 `mode:none` 返回空数组 `[]`，长度 0，所以创建也被拦截。

**修复**：改为 `当前权限缓存.length === 0` 时，只有 `工具名 !== '创建备忘录'` 才拦截。

#### 3. 卡片滑动竖滚延迟
**问题**：JS touchmove 绑在 document 上 + `passive:false`，每次竖滑浏览器要等 JS 处理完才滚。

**修复**：`passive:true` + CSS `touch-action: pan-y pinch-zoom` 协作，JS 只负责横滑视觉反馈，竖滚完全交浏览器原生处理。


### v0.2 部署步骤

1. `cp ~/Desktop/dev/love-helper/js模块/卡片滑动.js ~/liuyunji/entry/src/main/resources/rawfile/js模块/卡片滑动.js`
2. `cp ~/Desktop/dev/love-helper/js模块/智能体编辑面板.js ~/liuyunji/entry/src/main/resources/rawfile/js模块/智能体编辑面板.js`
3. `cp ~/Desktop/dev/love-helper/js模块/智能体管理.js ~/liuyunji/entry/src/main/resources/rawfile/js模块/智能体管理.js`
4. `cp ~/Desktop/dev/love-helper/js模块/API调用.js ~/liuyunji/entry/src/main/resources/rawfile/js模块/API调用.js`
5. `cp ~/Desktop/dev/love-helper/js模块/智能体权限向导.js ~/liuyunji/entry/src/main/resources/rawfile/js模块/智能体权限向导.js`
6. `cp ~/Desktop/dev/love-helper/css/智能体编辑面板-样式.css ~/liuyunji/entry/src/main/resources/rawfile/css/智能体编辑面板-样式.css`
7. `cp ~/Desktop/dev/love-helper/index.html ~/liuyunji/entry/src/main/resources/rawfile/index.html`

---

## 2026-05-07 — 项目启动 & 爱助手迁移

### 架构决策

**选定方案：WebView 嵌入（方案二）**
- 不重写 ArkTS 版，直接将 love-helper 的 HTML/JS/CSS 放入 `resources/rawfile/`
- 鸿蒙侧只负责 WebView 容器 + 后续通过 JSBridge 暴露原生能力（TTS/ASR）
- 无需过渡期，Web UI 仍可在浏览器独立开发调试

### 项目初始结构

```
liuyunji/
├── entry/src/main/
│   ├── ets/
│   │   ├── entryability/EntryAbility.ets
│   │   └── pages/Index.ets          ← WebView 容器
│   ├── resources/rawfile/
│   │   ├── index.html               ← 爱助手入口
│   │   ├── css/
│   │   │   ├── 样式.css
│   │   │   └── 智能体编辑面板-样式.css
│   │   └── js模块/                   ← 35 个 JS 模块
│   ├── module.json5                 ← 权限声明
│   └── resources/base/
└── ...
```

### 迁移操作记录

| 操作 | 详情 |
|------|------|
| 复制文件 | love-helper → rawfile，35 JS + 2 CSS + 1 HTML 全部对齐 |
| 网络权限 | `module.json5` 添加 `ohos.permission.INTERNET` |
| 重写 Index.ets | WebView 加载 `$rawfile('index.html')`，开启 `javaScriptAccess` + `domStorageAccess` |
| CSS 配置 | 修复 `EntryAbility.ets` ConfigurationConstant 导入（鸿蒙 API 15） |
| font-awesome | 注释掉（鸿蒙 rawfile 没有该文件） |
| navigator.onLine | 注入脚本强制 `true`（鸿蒙 WebView 默认可能 false，触发离线提示） |
| 创建开发快捷方式 | `流云记·开发测试.command` → HTTP server + Chrome 自动打开 |
| 百度搜索代理 | 将 `localhost:3778/proxy/baidu-search` 恢复为直连 `qianfan.baidubce.com/v2/ai_search/web_search` |

### 已知问题（待修复）

1. **百度联网搜索之前走代理** — 已修复为直连 API
2. **IndexedDB 数据不跨 origin** — `file://` 与 `http://localhost:3777` 数据库互相隔离
3. **font-awesome.min.css** — 暂时注释，图标可能缺失（但功能不受影响）

### 下一步计划

1. **TTS 原生能力** — 从 huawei6tts 项目引入 CoreSpeechKit textToSpeech（JSBridge 暴露）
2. **ASR 语音识别** — speechRecognizer 实现语音转文字输入
3. **后台音乐播放** — MediaSession + 后台进程保活（类似豆包通话体验）
4. **通话状态栏** — WebView 内通话悬浮窗 + 麦克风/扬声器切换

---

## 爱助手原项目日志

完整开发日志请见：
- `/Users/caixiaodan/Desktop/dev/love-helper/开发日志.md`（1328 行）
- `/Users/caixiaodan/Desktop/dev/love-helper/常见错误对照速查.txt`
